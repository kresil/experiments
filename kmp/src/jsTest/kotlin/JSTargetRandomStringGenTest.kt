/*
import kotlin.test.Test
import kotlin.test.assertEquals

class JSTargetRandomStringGenTest {

    @Test
    fun `test target object`() {
        val length = 10
        val (target, _) = TargetRandomStringGen(length).generate()
        assertEquals(Target.JS, target)
    }
}

// similar issue at: https://youtrack.jetbrains.com/issue/KTIJ-18311/MPP-IDE-Platform-specific-errors-are-reported-even-when-build-doesnt-target-that-platform
*/
